# Release Notes for v0.1.5

## Highlights
- Packages the current `main` line as `0.1.5`, while planned protobuf-breaking contract changes remain deferred to `2.0.0`.
- Adds the `/api/v1/query` server-side SQL endpoint backed by DataFusion with Arrow IPC or JSON output.
- Lands control-plane transaction support, flow cutover wiring, and orchestration output visibility lifecycle coverage across the current API and worker stack.

## Changelog Reference
- Change log entry: [`0.1.5`](../CHANGELOG.md#015---2026-04-11)

## Verification
- Local release-doc discipline check: `bash tools/check-release-tag-discipline.sh --tag v0.1.5`
- Release-tag CI discipline is enforced by `.github/workflows/ci.yml` (`Release Tag Discipline` job).
- SBOM publication and provenance attestation are enforced by `.github/workflows/release-sbom.yml`.
- Checked-in API contract can be refreshed with `cargo run -p arco-api --bin gen_openapi > crates/arco-api/openapi.json`.
