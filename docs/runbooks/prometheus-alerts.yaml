# Source of truth: `infra/monitoring/alerts.yaml`
#
# This file remains only as a historical pointer for older runbook references.
# Update and deploy alert rules from `infra/monitoring/alerts.yaml`, then use
# `promtool check rules infra/monitoring/alerts.yaml` plus the rule tests under
# `infra/monitoring/tests/` as the validation path.
