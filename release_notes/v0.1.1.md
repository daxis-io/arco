# Release Notes for v0.1.1

## Highlights
- Hardened Gate 1 release-tag discipline by enforcing changelog/release-note validation in tag-triggered CI.
- Fixed GitHub-hosted release discipline execution by installing `ripgrep` in release-tag and SBOM workflows.
- Closed the Gate 1 provenance + SBOM publication path with immutable evidence collection tied to this release tag.

## Changelog Reference
- Change log entry: [`0.1.1`](../CHANGELOG.md#011---2026-02-18)

## Verification
- Release-tag CI discipline is enforced by `.github/workflows/ci.yml` (`Release Tag Discipline` job).
- SBOM publication + retention + provenance attestation are enforced by `.github/workflows/release-sbom.yml`.
- Immutable release evidence pack is generated by `tools/collect_release_evidence.sh`.
