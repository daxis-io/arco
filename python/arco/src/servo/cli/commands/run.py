"""Run command implementation."""
from __future__ import annotations


def run_asset(
    *,
    asset: str,
    partitions: list[str],
    wait: bool,
    timeout: int,
) -> None:
    """Execute run command (stub)."""
    pass
