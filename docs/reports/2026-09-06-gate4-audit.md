# Gate 4 fresh independent audit

Reviewer: `/root/gate4_final_audit`, read-only reviewer agent, started with
`fork_turns: none`. The reviewer received the approved contract, exact base,
complete diff including new source files, source manifest, terminal logs,
operation/scaling reports, and a verified pre-audit checkpoint. It inherited no
implementation conversation, wrote no source, and ran no Cargo command.

The review below is the reviewer's final response. Its remaining administrative
steps are completed in the [final closeout](2026-09-06-gate4-closeout.md) and final
checkpoint. No source changes were required after this audit.

## Verdict

**Approve — local Gate 4.** I found no demonstrated P0, P1, or P2 defects and no materially unproven mandatory Gate 4 acceptance criterion.

This approval is limited to the local Gate 4 implementation and evidence. It does not grant merge, publication, deployment, or provider qualification authority.

## Confirmed findings

**None.**

## Acceptance coverage

- **Metadata-only transaction pinning:** Met. `begin_control_txn` validates scope, pins only the HEAD-selected transaction base, and initializes empty observations and staging at `crates/arco-catalog/src/state_store/control_mvp.rs:434-491`. `TransactionBase` retains authenticated manifest metadata and defers reconstruction until commit at `crates/arco-catalog/src/state_store/control_mvp/lazy.rs:25-149`.

- **Positive, negative, and tombstone observations:** Met. Selective point reads authenticate the relevant L1 block and L0 suffix at `control_mvp.rs:1157-1192`. Point witnesses, tombstone state, negative reads, memoization, and commit validation are implemented at `lazy.rs:312-440` and `lazy.rs:875-1005`. Staged overlay values are distinguished from base observations, so local writes do not weaken pinned-state validation.

- **Overlay scans, interval evidence, exhaustion, and transaction-local cursors:** Met. The resolved-row merge preserves newest-wins order across one L1 cursor and the L0 cursors at `lazy.rs:442-525`. Scan processing authenticates blocks under strict request budgets at `control_mvp.rs:5301-5407`; range fingerprints include tombstones and are cached only after complete coverage at `lazy.rs:641-707`. Pagination merges staged mutations before page limits and carries transaction nonce, pinned base, scope, prefix, and exclusive boundary at `lazy.rs:740-871`. Public authority continuation validation remains isolated from transaction-local continuation origins at `crates/arco-catalog/src/state_store.rs:1163-1380`.

- **Bounded streaming:** Met. The implementation keeps one block of rows per active source cursor and advances sources under explicit segment, block, and byte budgets. It does not materialize the complete logical scan before pagination.

- **Async outbox staging and trimming:** Met. Duplicate detection includes retained keyless-owner records and processes ordered L0 additions/trims at `lazy.rs:179-309`. Batch trim validation performs every awaited lookup before mutating transaction state, reserves capacity once, and checks exact `(record_id, origin_sequence)` incarnations at `control_mvp.rs:4245-4289`. Full replay independently rejects missing or stale incarnations and duplicate additions at `control_mvp.rs:5497-5577`.

- **Fresh pinned reconstruction and Gate 3 publication integrity:** Met. Normal commit always reconstructs the complete pinned state, validates observations and preconditions, and revalidates a promoted anchor before rendering at `control_mvp.rs:4304-4335`. The candidate transaction, history link, block/index rendering, replayed candidate state, optional anchor, physical root, manifest, and HEAD are completely validated and encoded before the first immutable PUT at `control_mvp.rs:4338-4521`. Replay also checks redundant anchor equivalence at `control_mvp.rs:699-705`.

- **Frozen-command conflict retry:** Met. Every CAS retry begins a fresh transaction, rereads the receipt, reapplies the frozen command, recomputes the predicted token, and regenerates receipt/audit/projection records at `crates/arco-catalog/src/catalog_authority.rs:3514-3568`. The regression test exercises a concurrent winning mutation and verifies the returned response and bound records derive from the successful retry.

- **Request accounting and nondurable lifetime:** Met. Required operation totals are recorded around whole operations, while phase counters are nested measurements. The supplied measurement audit verified 11,676 nested scalar phase values never exceed their enclosing totals, so the mandatory whole-operation claims do not sum overlapping phases or double count them. The lifecycle fixtures also establish that memoized selective reads remain nondurable and do not produce PUTs.

- **Executable cost bounds:** Met. Both scaling lanes contain all 17 required fixtures across block counts, L1 layouts, and L0 suffixes. Stable begin is exactly one HEAD plus two metadata GETs and zero data reads; immediate repeated point observations issue zero requests; overlay scans remain within the declared interval/block formulas; all 17 eager/lazy no-read lifecycle data-byte comparisons match; and the suffix-31 capacity case records zero PUT attempts for both paths. Original 256-setup/200-sample workload evidence remains present.

- **Compatibility and eager escape path:** Met. Public transaction trait shapes and serialized wire versions remain unchanged. Control format 7 and segment format 1 remain fixed at `control_mvp.rs:140-141`; authority continuations preserve the existing envelope while transaction-local cursor origins cannot be serialized as authority tokens. The eager reference remains test-only and the production path cannot select it.

- **Tests and oracle independence:** Met. The independent model test uses `LogicalOracle` and `LogicalTransaction`, exercises 16 seeds × 64 operations with binary and empty keys, tombstones, point/range assertions, scans, overlays, commits, and CAS loss at `crates/arco-catalog/tests/state_store_lazy_transactions.rs:20-159`. The multi-page L1/L0 overlay case is covered at the same file's `:162-226`. Focused tests cover corruption deferred until use, commit revalidation, missing directories, cursor isolation, genesis, dynamic/empty pages, eviction, cancellation-safe staging, exact outbox incarnations, retention/GC, maintenance CAS, and ABA-style delete/reinsert behavior.

## Evidence and independent checks

I reviewed the actual tracked diff and every untracked source file, rather than relying on the closeout summary. I independently confirmed:

- HEAD/base is `ca5023caa3ec1eb5b39edeef6291082f544a5153`.
- The complete candidate patch SHA-256 is `dfbb6834041df4aa9924febd71096c97f756357ee3ad490504ca215c988be4a5`.
- The verification manifest records all 492 tested Rust/Cargo inputs under aggregate `fe58a12ce53f9fca6922268a02aa44b49af2b811c44ca2056331edd5fd865c8e`.
- Every verification log hash matches the recorded report.
- The preserved relative-path scaling-report failure occurred after assertions during report output; the absolute-path rerun passed against the unchanged source aggregate.
- The recorded catalog, core, strict Clippy, formatting, diff, API all-targets, benchmark, reclamation, and both scaling lanes terminate successfully.

I did not rerun Cargo because this audit assignment explicitly reserved Cargo ownership to root.

## Plausible risks needing evidence

None within the Gate 4 contract rises to a defect or change request.

The evidence uses `MemoryBackend`; provider object-store consistency, latency, and conditional-write qualification remain outside this local gate and are not established by these results.

## Residual test gaps

- Final post-audit checkpoint creation and closeout completion remain root-owned administrative steps. Their pending state is not a code finding.
- Live provider and deployment evidence belongs to later gates.
- No additional independent Cargo execution was performed during this read-only audit; I instead authenticated the supplied command records, inputs, hashes, executable assertions, and reports.

**P0 actions:** none.

**P1 actions:** none.

**P2 actions:** none required for Gate 4 approval.
