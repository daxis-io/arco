# Release Notes for v0.1.2

## Highlights
- Fixed the Release SBOM workflow wait condition to read commit check-runs for `Release Tag Discipline`.
- Removed false timeout behavior where SBOM publication could fail even after successful tag-discipline CI.
- Preserved signed-tag validation, provenance attestation, SBOM publication, and immutable evidence collection gates.

## Changelog Reference
- Change log entry: [`0.1.2`](../CHANGELOG.md#012---2026-02-18)

## Verification
- Release-tag CI discipline is enforced by `.github/workflows/ci.yml` (`Release Tag Discipline` job).
- SBOM publication + retention + provenance attestation are enforced by `.github/workflows/release-sbom.yml`.
- Immutable release evidence pack is generated by `tools/collect_release_evidence.sh`.
